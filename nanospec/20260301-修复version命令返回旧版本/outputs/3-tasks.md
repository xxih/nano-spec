# 任务清单：修复 `nanospec --version` 返回旧版本

## 1. 代码修复

- [x] 1.1 修改 `src/index.ts`，将 CLI 版本号来源改为 `package.json`
- 验收条件：不再出现硬编码 `1.0.0` 作为 CLI 版本

## 2. 测试回归

- [x] 2.1 更新 `src/index.test.ts`，新增版本号与包版本一致断言
- 验收条件：相关单测通过并能防止版本硬编码回归

## 3. 文档与变更记录

- [x] 3.1 更新 `README.md`（命令表补充 `-V, --version`）
- [x] 3.2 更新 `CHANGELOG.md` Unreleased
- 验收条件：文档与实现一致，变更记录完整

## 4. 验证

- [x] 4.1 运行变更路径测试并确认通过
- [x] 4.2 完成后回填任务状态
- 验收条件：测试通过，任务状态可恢复追踪
