# skills 跨工具安装指南

目标：在 README 增加“尽量兼容任意 AI Agent 工具”的 skills 安装与分发指引。

要求：
1. 说明开放标准最小结构（`SKILL.md` + YAML frontmatter 的 `name` / `description`）。
2. 给出基于 GitHub 的分发方式与目录约定。
3. 给出 Codex 一键安装命令（`$skill-installer install <github-subdir-url>`）。
4. 给出对非原生 skills 工具的兜底建议（如 MCP/CLI 转接）。
5. 指引保持实用、可复制执行。
