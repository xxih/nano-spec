# 归档：npm 发布和 GitHub CI/CD 配置指南（2026-01-20）

本文件已归档，不再作为当前流程依据。

## 归档原因

- 内容过长，且包含多个互斥方案（手动发布、semantic-release、OIDC-only）。
- 与当前仓库实际 workflow（`.github/workflows/release.yml`）存在偏差。

## 当前应使用的文档

- 请使用 [CI-CD完整方案.md](./CI-CD完整方案.md) 作为唯一权威发布指南。

## 保留范围

- 仅保留历史背景，不用于执行发布。
